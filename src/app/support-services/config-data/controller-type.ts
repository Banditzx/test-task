// This enum consists of the project controllers and is used to create a requests in the RequestHelperService.
export enum Controller
{
    AUTH = "Auth",
    ACCOUNT = "Account",
    LOCATION = "Location"
}